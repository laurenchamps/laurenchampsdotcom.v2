---
import '@astrojs/tailwind/base.css'
import '../assets/scss/globals.scss'
import SiteMeta from '../components/SiteMeta.astro'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'

const {
  title = 'Lauren Champs',
  description = 'I help business owners streamline systems and turn chaos into calm.',
  url = Astro.site,
  image = 'logo-brick.png',
  author = 'Lauren Champs',
} = Astro.props
---

<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />

    <!-- favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon-brick.svg" />

    <SiteMeta title={title} description={description.substring(0, 100)} url={url} image={image} author={author} />
    <!-- Google tag (gtag.js) -->
    <script type="text/partytown" async src="https://www.googletagmanager.com/gtag/js?id=G-ENPWDYP70L"></script>
    <script type="text/partytown">
      window.dataLayer = window.dataLayer || []
      function gtag() {
        dataLayer.push(arguments)
      }
      gtag('js', new Date())

      gtag('config', 'G-ENPWDYP70L')
    </script>
  </head>
  <body>
    <Header />
    <main id="main-content" transition:animate="fade">
      <slot />
    </main>
    <Footer />
    <style lang="scss" is:global>
      // theme settings
      :root {
        --radius-small: 3px;
        --radius-large: 6px;
        --gap-default: 2rem;
        --font-measure: 70ch;
        --font-family-default: 'Satoshi', sans-serif;
        // --font-family-special: 'Hedvig Letters Serif', sans-serif;
        --font-family-special: 'Satoshi', sans-serif;
        // light theme
        --font-color: var(--color-dark-text);
        --action-color: var(--color-primary);
        --action-color-state: var(--color-primary);
        --bright-color: var(--color-accent);
        --background: var(--color-light);
        --primary-background: var(--color-light);
        --neutral-background: var(--color-mid);
      }

      // dark color scheme overrides
      .darkmode {
        --font-color: var(--color-light);
        --action-color: var(--color-primary);
        --action-color-state: var(--color-primary);
        --background: var(--color-dark);
        --primary-background: var(--color-dark);
        --neutral-background: var(--color-mid);
        --ribbon-color: var(--color-primary);
        --logo-name: var(--color-mid);
        --logo-loop: var(--color-mid);
        --logo-badge: var(--color-accent);
      }

      .feature svg path {
        fill: var(--color-dark);
      }

      svg .name {
        fill: var(--logo-name);
      }

      svg .loop {
        fill: var(--logo-loop);
      }

      svg .badge {
        fill: var(--logo-badge);
      }

      // sticky footer on low content pages
      html,
      body {
        height: 100%;
      }

      body {
        display: flex;
        flex-direction: column;
        background-color: var(--color-light);
        overflow-x: hidden;
        color: var(--font-color);

        main {
          flex: 1 0 auto;
        }

        footer {
          flex-shrink: 0;
        }
      }

      pre {
        border: 2px solid var(--action-color);
        border-radius: 0.35rem;
        padding: 1rem;
      }

      body .pagination a {
        &:hover,
        &:focus-visible {
          background-color: var(--action-color-state);

          svg path {
            stroke: white;
          }
        }
      }

      .sr-only {
        position: absolute;
        margin: -1px;
        padding: 0;
        width: 1px;
        height: 1px;
        overflow: hidden;
        clip: rect(0, 0, 0, 0);
        border-width: 0;
        white-space: nowrap;
      }
    </style>
  </body>
</html>
